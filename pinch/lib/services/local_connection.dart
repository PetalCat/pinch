import 'mock_connection.dart';

/// Stub for real HTTP + WebSocket implementation (Task 19).
/// Extends MockConnectionService so the app is functional in the meantime.
class LocalConnection extends MockConnectionService {}
